phpcs:
  stage: lint
  needs:
    - composer
  script:
    - vendor/bin/phpcs --report=junit > ./storage/logs/phpcs-junit.xml
  artifacts:
    when: always
    paths:
      - storage/logs
    reports:
      junit: storage/logs/phpcs-junit.xml
